dependencies:
  override:
    - make install.hack

test:
  #pre: make lint

  override:
    - make test

deployment:
  production:
    branch: master
    commands:
      - echo "upload to github releases or bintray"
